  <%= form_for([blog, comment], remote: true) do |f| %>
  <%= f.hidden_field :blog_id %>
 <div class="field">
   <%= f.text_field :content, placeholder: "内容", class: "form-control"  %>
 </div>
 <div class="actions comment_action">
   <%= f.submit "投稿する" %>
   </div>
<% end %>
